# SCAFAD Core Requirements
# ========================
# Production dependencies for SCAFAD framework
# Generated: 2026-04-20 (WP-2.2 reconciliation)
#
# Categories
#   [core]     — required for the main pipeline to run
#   [ml]       — machine learning; some optional (graceful degradation)
#   [eval]     — evaluation scripts and visualisation
#   [aws]      — AWS integration; degraded gracefully in local dev
#   [dev]      — development and testing tools

# [core] Scientific computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.4.0

# [core] Graph analysis
networkx>=2.8

# [core] Schema validation and packaging utilities
jsonschema>=4.0.0
packaging>=21.0

# [core] AWS SDK
boto3>=1.24.0
botocore>=1.27.0
aiohttp>=3.8.0

# [core] Async processing
aiofiles>=0.8.0

# [core] Configuration
pyyaml>=6.0

# [core] System monitoring
# graceful degradation: resource metrics unavailable without psutil
psutil>=5.9.0

# [core] Cryptographic signing
# graceful degradation: telemetry signature validation disabled without cryptography
cryptography>=38.0.0

# [ml] Machine learning
scikit-learn>=1.1.0

# [ml] Deep learning (optional)
# graceful degradation: PyTorch-based detectors fall back to classical baselines
torch>=1.12.0

# [ml] Graph neural networks (optional; requires torch)
# graceful degradation: GNN detectors disabled without torch-geometric
torch-geometric>=2.0.0

# [ml] JIT acceleration (optional)
# graceful degradation: numba JIT disabled; pure-Python path used instead
numba>=0.56.0

# [ml] Statistical baselines (optional)
# graceful degradation: classical statistical detectors unavailable without statsmodels
statsmodels>=0.13.0

# [eval] Visualisation and evaluation
matplotlib>=3.5.0
seaborn>=0.12.0

# [compression] Algorithm alternatives (optional)
# graceful degradation: lz4/brotli/snappy fall back to zlib
lz4>=4.0.0
brotli>=1.0.9
python-snappy>=0.6.0

# [aws] Lambda deployment helpers (optional in local dev)
# graceful degradation: Lambda runtime interface client unavailable outside Lambda
awslambdaric>=1.0.0
# graceful degradation: X-Ray tracing disabled without aws-xray-sdk
aws-xray-sdk>=2.10.0

# [dev] Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# [dev] Type hints
typing-extensions>=4.0.0
